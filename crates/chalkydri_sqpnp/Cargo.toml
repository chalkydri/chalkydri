[package]
name = "chalkydri_sqpnp"
version = "0.1.1"
edition = "2024"

[dependencies]
nalgebra = "0.34.1"
rerun = { version = "0.30.2", optional = true }
tracing.workspace = true
uom = "0.38.0"

[features]
default = ["rerun"]
rerun = ["dep:rerun"]
